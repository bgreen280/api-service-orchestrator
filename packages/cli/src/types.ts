import { Playlist as YouTubePlaylist, PlaylistItem as YouTubePlaylistItem } from '@apiso/youtube';
// import { Raindrop } from '@apiso/raindrop';

export interface CommandResult {
  success: boolean;
  message: string;
}